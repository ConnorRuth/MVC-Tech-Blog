<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Created by {{user.name}} on {{ date_created}}</p>
</div>

{{#each commentData as |comment| }}
<div class="row mb-4 project">
  <div class="col-md-5">

    <p>{{comment.content}}</p>
  </div>
  <div class="col-md-7">
    <p>
      Created by {{comment.user.name}} on {{project.date_created}}
    </p>
  </div>
</div>
{{/each}}